<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="@dimen/fourty_eight_dp"
    tools:context="comw.example.mdsaif.packagedrinkingapp.activity.CartActivity"
    tools:showIn="@layout/activity_cart">

    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/recycler_view"></android.support.v7.widget.RecyclerView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/fourty_eight_dp"
        android:layout_alignParentBottom="true">

        <TextView
            android:layout_width="@dimen/zero_dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:hint="Total"
            android:textColor="@color/white"
            android:id="@+id/subtotal_view"
            android:background="#4757a9f1"/>

        <TextView
            android:layout_width="@dimen/zero_dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/proceed"
            android:id="@+id/proceed_btn"
            android:textColor="@color/white"
            android:background="@drawable/button_gradient"/>

    </LinearLayout>

</RelativeLayout>
